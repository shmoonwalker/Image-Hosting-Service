spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/image_hosting}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER}
    password: ${RABBITMQ_PASSWORD}

    listener:
      simple:
        default-requeue-rejected: false
        retry:
          enabled: true
          max-attempts: 3
          initial-interval: 2s
          multiplier: 2.0
          max-interval: 10s

  session:
    jdbc:
      initialize-schema: always

server:
  servlet:
    session:
      cookie:
        secure: false
        same-site: lax

logging:
  level:
    root: INFO
    com.example.imagehostingservice: DEBUG
    software.amazon.awssdk: WARN

management:
  endpoint:
    health:
      show-details: always

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs

  swagger-ui:
    enabled: true
    path: /swagger-ui.html

app:
  frontend:
    reset-password-url: ${RESET_PASSWORD_URL:http://localhost:5173/reset-password}

resend:
  api-key: ${RESEND_API_KEY}
  from-email: "${RESEND_FROM_EMAIL:Image Hosting Service <no-reply@paiksa.com>}"
  password-reset-template: ${RESEND_PASSWORD_RESET_TEMPLATE:password-reset}
